#include <functional>
#include <iostream>
#include <map>
#include <memory>
#include <set>
#include <stdio.h>
#include <string>
#include <string_view>
#include <vector>

int main() {
  uint32_t p = 2;
  uint32_t pp=4;
  printf("%d %d", sizeof(p),sizeof(pp));
  std::string_view p;
  return 0;
}